# 📈 Sales Analysis Manager (Go Project)

## 💡 Огляд Проекту

**Sales Analysis Manager** — це консольна програма, написана на **Go (Golang)**, яка демонструє ключові навички роботи з I/O, обробкою файлів та структурами даних.

Основне завдання програми:

1.  Читання та парсинг даних про продажі з файлу формату **CSV**.
2.  **Валідація та очищення даних** (пропуск некоректних записів).
3.  Проведення **базового аналізу** (загальний дохід, найпопулярніший продукт).
4.  Виведення структурованого звіту в консоль.

Проект використовує **стандартну бібліотеку Go** (`encoding/csv`, `strconv`, `flag`) і відповідає рекомендованій структурі проекту `cmd/internal`.

-----

## 🛠️ Структура Проекту та Залежності

### Стек Технологій

* **Мова:** Go (Golang)
* **Залежності:** Виключно **стандартна бібліотека Go**.

### Структура Директорій

```
sales-analysis/
├── cmd/
│   └── sales-analysis/
│       └── main.go       # Точка входу: обробка аргументів (пакет flag) та виклик логіки
├── internal/
│   └── analyzer/
│       ├── analyzer.go   # Бізнес-логіка: структури, парсинг CSV, аналіз
│       └── analyzer_test.go# Юніт-тести для логіки парсингу та аналізу
├── data/
│   └── sales.csv         # Приклад вхідних даних
├── go.mod
└── README.md
```

-----

## 🚀 Запуск Проекту

### Вимоги

* Встановлений **Go (версія 1.18+)**.
* Ініціалізований модуль Go (`go mod init sales-analysis`).

### 1\. Збірка та Запуск

Виконайте команду з кореневої папки проекту, вказавши шлях до вхідного CSV-файлу за допомогою прапора `--file`:

```bash
go run cmd/sales-analysis/main.go --file=data/sales.csv
```

### 2\. Використання Значення За Замовчуванням

Оскільки прапор `--file` має значення за замовчуванням (`data/sales.csv`), ви можете запустити програму без аргументів:

```bash
go run cmd/sales-analysis/main.go
```

### 3\. Перегляд Довідки

Користувач може побачити опис вашого аргументу за допомогою прапора `--help` (завдяки пакету `flag`):

```bash
go run cmd/sales-analysis/main.go --help
```

-----

## 🧪 Тестування

Проект містить **юніт-тести** для всіх ключових функцій парсингу та аналізу, включаючи перевірку на коректну обробку помилок (пропуск невалідних рядків).

Виконайте команду для запуску тестів:

```bash
go test ./...
```

**Очікуваний успішний вивід:**

```
?       sales-analysis/cmd/sales-analysis       [no test files]
ok      sales-analysis/internal/analyzer        0.0XXs
```

-----

## 📊 Приклад Виводу

На основі коректного файлу `sales.csv` програма виведе структурований звіт:

```
--- Sales Record Analysis Report ---
File Processed: data/sales.csv
Total Valid Transactions: 4
Total Revenue:  4236.40 $
Most Popular Product: Mouse (sold 10 units)
------------------------------------
```